#include<stdio.h>
void main()
{
int a[100], n, i;
printf("Enter the no. of array elements ");
scanf("%d",&n);
printf("Enter the elements :::");
for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
for(i=0;i<n;i++)
  {
    printf("%d",a[i]);
  }
  return 0;
}
